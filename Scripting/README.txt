# This command processes that data in Hadoop
./makeHadoopSU <path of SEGY dir> <path of HDFS dir> <path of HDFS for reduced data> 

# This command processes the data on local system using Seismic Unix
./makeSU <path of SEGY dir>